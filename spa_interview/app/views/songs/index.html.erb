<p id="notice"><%= notice %></p>

<div class="container">

  <div class="row">
    <h1>Hi im Alex...</h1>

    <h2>Welcome to my play list!</h2>
    <p class="lead">This is just a super simple application built on rails in which I can add and remove songs from my play list.
      Feel free to give these puppies a listen.</p>

  </div>
  <div class="row">

    <h3>Songs</h3>

    <table class="table table-striped">
      <thead>
      <tr>
        <th>Title</th>
        <th>Artist</th>
        <th colspan="3"></th>
      </tr>
      </thead>

      <tbody>
      <% @songs.each do |song| %>
          <tr>
            <td><%= song.title %></td>
            <td><%= song.artist %></td>
            <td><%= link_to 'Show', song %></td>
            <td><%= link_to 'Edit', edit_song_path(song) %></td>
            <td><%= link_to 'Destroy', song, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
      <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'New Song', new_song_path , class: 'btn btn-lg btn-success'%>

  </div>




</div>
